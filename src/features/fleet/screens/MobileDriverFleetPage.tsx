import DriverVehicleScreen from "./DriverVehicleScreen";

/** Flotte côté conducteur — véhicule assigné et actions rapides. */
export default function MobileDriverFleetPage() {
  return <DriverVehicleScreen />;
}
