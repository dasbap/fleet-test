/**
 * Extension future : wrappers typés pour les plugins Capacitor (@capacitor/camera,
 * @capacitor/geolocation, @capacitor/push-notifications, Preferences, Share, App, etc.).
 * V1 : aucune dépendance native supplémentaire.
 */

export {};
