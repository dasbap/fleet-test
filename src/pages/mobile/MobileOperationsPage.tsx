import { OperationsHubScreen } from "@/features/operations";

/** Onglet Opérations — raccourcis métier (items selon le rôle dans OperationsHub). */
export default function MobileOperationsPage() {
  return <OperationsHubScreen />;
}
