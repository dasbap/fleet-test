import { AlertsHubScreen } from "@/features/alerts";

/** Onglet Alertes — liste et traitement des alertes (contenu commun web / mobile). */
export default function MobileAlertsPage() {
  return <AlertsHubScreen />;
}
