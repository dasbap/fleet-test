import FleetListScreen from "./FleetListScreen";

/**
 * Flotte (gestionnaires / organisateurs / mécaniciens) — liste des véhicules.
 * Les conducteurs utilisent la route `my-vehicle` (écran dédié).
 */
export default function MobileFleetPage() {
  return <FleetListScreen />;
}
