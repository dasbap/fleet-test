# Local Agent Notes

- Tests should verify user-facing behavior or important repository/service contracts.
- Keep fixtures minimal and deterministic.
- Prefer targeted test runs while iterating, then broaden verification when the touched area is shared.
