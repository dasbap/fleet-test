# Local Agent Notes

- This folder contains app workspaces. Check for nested `AGENTS.md` files before editing an app.
- Keep framework-specific conventions inside each app rather than leaking them into shared code.
- Avoid cross-app refactors unless the user asks for them.
