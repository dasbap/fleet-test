# Local Agent Notes

- Packages are shared contracts and libraries. Keep public APIs stable unless a breaking change is intentional.
- Prefer adding tests near the package that owns the behavior.
- Avoid importing app-only code from packages.
